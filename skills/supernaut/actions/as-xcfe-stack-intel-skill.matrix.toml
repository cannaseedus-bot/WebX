[skill]
name        = "as-xcfe-stack-intel"
version     = "1.0.0"
description = "AS-XCFE stack intelligence — cross-stack introspection, skill gap analysis, and sync validation"
runtime     = "xcfe"
shard_checkpoint = false

[actions]
diff_stacks      = { entry = "AsXcfeStackIntelActions.diff_stacks",      runtime = "xcfe" }
list_agents      = { entry = "AsXcfeStackIntelActions.list_agents",      runtime = "xcfe" }
validate_sync    = { entry = "AsXcfeStackIntelActions.validate_sync",    runtime = "xcfe" }
report_gaps      = { entry = "AsXcfeStackIntelActions.report_gaps",      runtime = "xcfe" }
sync_skill       = { entry = "AsXcfeStackIntelActions.sync_skill",       runtime = "xcfe" }
